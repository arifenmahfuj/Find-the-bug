const config = {
  apiBaseUrl: "https://api.training-example.invalid",
  internalService: "http://internal-service.training.invalid",
  demoToken: "TRAINING_TOKEN_DO_NOT_USE"
};

module.exports = config;
